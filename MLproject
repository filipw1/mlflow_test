name: Test_train

entry_points:
  main:
    parameters:
      alpha: {type: float, default: 0.1}
      lambda: {type: float, default: 0.1} 
    command: "Rscript test_model.R -P alpha={alpha} lambda={lambda}"